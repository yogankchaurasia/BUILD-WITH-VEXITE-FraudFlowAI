# Copy to .env if you want to override defaults (not auto-loaded unless you
# add python-dotenv and load it in main.py — kept simple for the demo).
DATABASE_URL=sqlite:///./fraudflow.db
JWT_SECRET_KEY=change-me-in-production
